<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.web.sollabo.mybatis.cart.CartMapper">

	<!-- 카트 추가 -->
	<insert id="addCart">
	
		insert into cart(MEM-NO, PRODUCT_NO,  product_order_count)
		values(#{MEM-NO}, #{PRODUCT_NO}, #{ product_order_count}) 
	
	</insert>
	
	
	<!-- 카트 삭제 -->
	<delete id="deleteCart">
	
		delete from cart where cartNo = #{CART_No}
	
	</delete>
	
	<!-- 카트 수량 수정 -->
	<update id="modifyCount">
	
		update cart set CART_CNT=#{CART_CNT} where cart_NO = #{cart_NO}
	
	</update>
	
	
	<!-- 카트 목록 -->
	<select id="getCart" resultType="com.web.sollabo.cart.dto.CartDTO">
	
		select a.CART-NO, a.MEM_NO, a.PRODUCT_NO, a.product_order_count, b.product_name, b.product_Price, <!-- b.product_Discount -->
		from cart a left outer join product b on a.bookId = b.bookId
		where MEM_NO = #{MEM_NO}	
	
	</select>
	
	<!-- 카트 확인 -->
	<select id="checkCart" resultType="com.web.sollabo.cart.dto.CartDTO">
	
		select * from cart 
		where MEM_NO = #{MEM_NO} and PRODUCT_NO = #{PRODUCT_NO}
	
	</select>


</mapper>